<%= form_for task, method: :patch do |f| %>
  <% if task.completed %>
    <%= f.text_field :description, class: 'completed' %>
  <% else %>
    <%= f.text_field :description %>
  <% end %>
  <%= f.submit 'Update' %>
<% end %>
